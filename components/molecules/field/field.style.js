import styled from '@emotion/styled';

export default styled.div`
  margin: 6px 0;

  .checkbox-wrapper {
    .info-note {
      margin-left: 22px;
      user-select: text;
    }
  }

  .checkbox-wrapper.size-lg .info-note {
    margin-left: 28px;
  }
`;
